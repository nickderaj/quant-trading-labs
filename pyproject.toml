[project]
name = "quant-trading-labs"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "jupyter",
    "numpy>=2.4.6",
    "pandas",
    "python-binance",
    "torch>=2.13.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "ruff>=0.16.0",
]
